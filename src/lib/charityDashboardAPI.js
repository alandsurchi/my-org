// Legacy alias file to ensure compatibility and "only one source of truth"
// All API calls are now routed through the unified apiClient.ts
import apiClient from './apiClient';

export default apiClient;
